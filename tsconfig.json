{
  "compilerOptions": {
    "module": "ESNext",          // 推荐
    "moduleResolution": "Node",  // 关键
    "esModuleInterop": true,     // 允许 CommonJS/ES 模块互操作
    "types": ["vite/client","node"],    // 识别 Vite 环境变量
    "baseUrl": "./",             // 解析基础路径
    "lib": ["ES2015", "DOM"],
    "paths": {
      "@/*": ["src/*"]           // 别名映射（若使用）
    }
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.vue"] // 包含所有需要编译的文件
}